# Copy to .env and adjust. Used by the API / future DB integration.
MODEL_PATH=models/risk_model.joblib
API_PORT=8000
# Postgres (for persisting applications + audit trail in production)
DATABASE_URL=postgresql://insurance:insurance@db:5432/insurance
MLFLOW_TRACKING_URI=file:./mlruns
