# ---- Core (required to train + run the pipeline) ----
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
joblib>=1.3
PyYAML>=6.0

# ---- Serving ----
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# ---- Recommended for production (pipeline degrades gracefully without them) ----
xgboost>=2.0          # preferred model backend
shap>=0.44            # per-decision explanations
mlflow>=2.10          # experiment tracking + model registry
evidently>=0.4        # richer drift monitoring
pandera>=0.18         # declarative data validation

# ---- Dev ----
pytest>=8.0
ruff>=0.3
httpx>=0.27           # required by fastapi TestClient
